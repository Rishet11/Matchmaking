services:
  - type: web
    name: matchmaking
    env: python
    buildCommand: chmod +x build.sh && ./build.sh
    startCommand: gunicorn server:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0 